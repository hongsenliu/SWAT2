@model SWAT.Models.lkpswatscorevarslu

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>lkpswatscorevarslu</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.VarName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.VarName)
            @Html.ValidationMessageFor(model => model.VarName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SectionID, "lkpSWATSectionLU")
        </div>
        <div class="editor-field">
            @Html.DropDownList("SectionID", String.Empty)
            @Html.ValidationMessageFor(model => model.SectionID)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
